#
# 5BSD default kernel configuration.
#
# GENERIC plus 5BSD identity.  The cap_rt capability runtime and
# Linux compatibility modules are loaded at boot via loader.conf.
#

include GENERIC

ident		VBSD
