#
# 5BSD default kernel configuration.
#
# GENERIC plus 5BSD identity.  The mac_capability MAC capability and
# Linux compatibility modules are loaded at boot via loader.conf.
#

include GENERIC

ident		VBSD

# 5BSD is 64-bit only: no 32-bit binary compatibility.
nooptions	COMPAT_FREEBSD32
