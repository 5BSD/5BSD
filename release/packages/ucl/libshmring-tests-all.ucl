comment = "Capability-safe shared-memory ring tests"

desc = <<EOD
Tests for the shared-memory ring protocol, descriptor rights, corruption
detection, and capability-mode operation.
EOD

annotations = {
	set = "tests"
}
