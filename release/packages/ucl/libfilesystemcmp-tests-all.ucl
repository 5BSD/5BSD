comment = "FileSystemCmp protocol tests"

desc = <<EOD
ABI, malformed-frame, descriptor-cardinality, typed-discovery, handle, and
public FileSystemCmp client API tests.
EOD

annotations = {
	set = "tests"
}
