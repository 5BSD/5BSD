comment = "Capability service client library tests"

desc = <<EOD
Tests for capability service initialization, descriptor authorization,
typed local/global discovery, concurrent registry dispatch and accept,
fork isolation, component injection, and provider bootstrap validation.
EOD

annotations = {
	set = "tests"
}
