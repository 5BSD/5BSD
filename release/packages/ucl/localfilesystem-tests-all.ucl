comment = "FileSystemCmp provider tests"

desc = <<EOD
Protocol-negative, model, quota, stale-handle, persistent-restart, hard-link
confinement, per-file reconstruction, read-only-bundle, sandbox, and serviced
integration tests for FileSystemCmp.
EOD

annotations = {
	set = "tests"
}
