comment = "Capability-mode TraceCmp provider"

desc = <<EOD
The TraceCmp provider and verified /Capabilities/System/TraceCmp.cap bundle.
It delegates one broker-opened DTrace descriptor across a bounded two-hop
transfer path to an explicitly authorized consumer.
EOD
