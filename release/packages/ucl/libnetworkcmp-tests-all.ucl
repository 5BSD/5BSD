comment = "NetworkCmp protocol tests"

desc = <<EOD
ABI, malformed-frame, descriptor-cardinality, typed-discovery, endpoint, and
public NetworkCmp client API tests.
EOD

annotations = {
	set = "tests"
}
