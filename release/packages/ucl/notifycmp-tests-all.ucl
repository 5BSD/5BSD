comment = "NotifyCmp provider tests"

desc = <<EOD
Routing, identity, queue pressure, timer, sandbox, bundle, audit, and DTrace
tests for the NotifyCmp provider.
EOD

annotations = {
	set = "tests"
}
