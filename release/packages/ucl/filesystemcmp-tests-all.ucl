comment = "FileSystemCmp provider tests"

desc = <<EOD
Protocol-negative, model, quota, stale-handle, persistent-restart,
read-only-bundle, sandbox, and serviced integration tests for FileSystemCmp.
EOD

annotations = {
	set = "tests"
}
