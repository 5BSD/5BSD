WORLDPACKAGE=	libcapsulert-tests
SUBPACKAGES=	dbg
PKG_SETS=	tests

PKG_DEPS.libcapsulert-tests+=	atf
PKG_DEPS.libcapsulert-tests+=	kyua
PKG_DEPS.libcapsulert-tests+=	set-base
PKG_DEPS.libcapsulert-tests+=	libcapsulert

.include <bsd.pkg.mk>
