WORLDPACKAGE=	libauthorityctl-tests
SUBPACKAGES=	dbg
PKG_SETS=	tests

PKG_DEPS.libauthorityctl-tests+=	atf
PKG_DEPS.libauthorityctl-tests+=	kyua
PKG_DEPS.libauthorityctl-tests+=	set-base
PKG_DEPS.libauthorityctl-tests+=	libauthorityctl

.include <bsd.pkg.mk>
