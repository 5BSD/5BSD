WORLDPACKAGE=	oraclectl-tests
SUBPACKAGES=
PKG_SETS=	tests

PKG_DEPS.oraclectl-tests+=	atf
PKG_DEPS.oraclectl-tests+=	kyua
PKG_DEPS.oraclectl-tests+=	oraclectl
PKG_DEPS.oraclectl-tests+=	oracled
PKG_DEPS.oraclectl-tests+=	set-base

.include <bsd.pkg.mk>
