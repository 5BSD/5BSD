WORLDPACKAGE=	libauthorityrt-tests
SUBPACKAGES=	dbg
PKG_SETS=	tests

PKG_DEPS.libauthorityrt-tests+=	atf
PKG_DEPS.libauthorityrt-tests+=	kyua
PKG_DEPS.libauthorityrt-tests+=	set-base
PKG_DEPS.libauthorityrt-tests+=	libauthorityrt

.include <bsd.pkg.mk>
