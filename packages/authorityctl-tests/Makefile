WORLDPACKAGE=	authorityctl-tests
SUBPACKAGES=
PKG_SETS=	tests

PKG_DEPS.authorityctl-tests+=	atf
PKG_DEPS.authorityctl-tests+=	kyua
PKG_DEPS.authorityctl-tests+=	authorityctl
PKG_DEPS.authorityctl-tests+=	authorityd
PKG_DEPS.authorityctl-tests+=	set-base

.include <bsd.pkg.mk>
