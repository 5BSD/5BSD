activation { boot = true; ipc = ["system.Log"]; }
program = "Log";
restart = "on-failure";
user = "capability";

# Pre-exec containment: the logger holds many segment files, so a roomier
# descriptor ceiling; no core dumps (may hold buffered log data).
limits { nofile = 2048; nproc = 64; core = 0; }
umask = "0077";

storage = [{
    name = "state";
    scope = "unit";
    lifetime = "persistent";
    rights = "mount";
}];
