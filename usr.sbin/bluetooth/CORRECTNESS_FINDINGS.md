# Bluetooth stack — functional correctness review (post-fix)

Correctness-only pass (NOT security) over blued/meshd/libmesh, run AFTER commit
`df027e4bcea` (the audit-fix pass). Special attention to regressions introduced
by that commit, since those fixes were syntax-checked but never run.

Legend: **REGRESSION** = introduced by df027e4bcea; **PRE-EXISTING** = older bug.

Streams: 1 SMP ✅ · 2 ATT/GATT ✅ · 3 HCI/GAP ✅ · 4 mesh crypto/net ✅ · 5 mesh models/DF ✅ · 6 meshd+friend/mgr ✅ — ALL COMPLETE

---

## Stream 1 — SMP / pairing

### HIGH
- [x] **C1-H1 (REGRESSION) — `kp_negotiated` never set on SC passkey path; keypress callback dead.** ✅ FIXED (set `kp_negotiated=(preq[3]&pres[3]&KEYPRESS)` before use_sc dispatch in both smp_pair/smp_respond; legacy dups removed) `smp.c:308` gates inbound Keypress delivery on `sc->kp_negotiated`, but it's only assigned in the two *legacy* passkey branches (`smp.c:1275,1997`). SC dispatch runs earlier (`smp.c:1217-1226` init, `1948-1956` resp) with the flag still false. SC Passkey Entry (the common MITM path) → keypress PDUs consumed but `keypress_cb` never fires (pre-df027e4 code fired it unconditionally). Fix: set `sc->kp_negotiated = (preq[3]&pres[3]&SMP_AUTH_KEYPRESS)` before the `use_sc` dispatch in both `smp_pair()` and `smp_respond()`.
- [ ] **C1-H2 (PRE-EXISTING) — Local SC-OOB ephemeral cleared before the pairing that needs it.** `blued_oob_take()` (`ctl.c:1372`) calls `smp_sc_oob_clear_local()` while consuming the OOB entry, BEFORE `smp_pair_sc()` → `smp_sc_gen_ephemeral()` (`smp_sc.c:747`). Hook already NULL → fresh random ephemeral, wire PKa ≠ the pkx handed to peer OOB → peer's `Ca=f4(PKax,PKax,ra,0)` always fails → every SC-OOB pairing rejected with Confirm Value Failed. Fix: clear after the ephemeral is consumed, not at OOB-take.

### MEDIUM
- [x] **C1-M1 (PRE-EXISTING) — OOB flag advertised regardless of which pairing type the OOB supports.** ✅ FIXED (initiator legacy-OOB abort falls back to IO-cap model + re-enforces floor; responder abort deliberately left — fallback there would mis-mark JW bond as authenticated) `smp.c:969-971,1736-1738` set OOB flag if *either* legacy or SC OOB present; if negotiation falls to the other flavor, `SMP_MODEL_OOB` selected then aborts (`smp.c:1233-1239` / `smp_sc.c:851-857,1310-1316`). A pairing that could complete (JW/NC) deterministically fails.
- [x] **C1-M2 (PRE-EXISTING) — One-sided SC OOB (legal, Table 2.7) always rejected instead of continuing with r=0.** ✅ FIXED (init+resp: skip peer-confirm, proceed with r=0 per §2.3.5.6.4) `smp_sc.c:851,1310` hard-fail `OOB_NOT_AVAILABLE`; Core Vol 3 Part H §2.3.5.6.4 lets a device with no received OOB proceed (skip peer-confirm, r=0 in f6). Asymmetric OOB → interop stacks pair, this refuses.

### LOW — ✅ ALL FIXED
- [x] **C1-L1 — Central's distributed legacy EncKey discarded.** ✅ FIXED (record ltk/rand/ediv into bond when `!has_ltk`, preserving peripheral's key when both distribute; added bond param to smp_distribute_init_keys)
- [x] **C1-L2 — SC key-dist failure aborts silently.** ✅ FIXED (all 4 SC sites send Pairing Failed before cleanup)
- [x] **C1-L3 (doc) — `smp_bond_db_import` returns -2 but header documents 0/1/-1.** ✅ FIXED (header documents -2)
- [x] **C1-L4 — stale errno after keypress flood.** ✅ FIXED (errno=EPROTO before -1)

### Verified CORRECT (stream 1)
S-M3 signed-write byte order self-consistent + matches BlueZ/Zephyr (wire MAC = reverse(full_mac)[0..7]); call-site framing (msg=opcode‖handle‖value, counter sig[0..3], MAC sig[4..11]) correct. `smp_swap_buf` correct for dst==src and disjoint at every call site. Crypto primitives (c1/s1/f4/f5/f6/g2/h6/h7/ah/CTKD salts+keyids) message layouts correct. All-zero-IRK reject (S-M1) at 4 sites; S-M2 continuity guard + memmove rollback correct; `preq[6]&pres[6]` at 3 init sites; S-m6/S-m8 correct. State machines (legacy + SC JW/NC/OOB/passkey ordering, 20-round loop, Ea/Eb order, model tables vs Table 2.8, §3.4 timer). Bond DB encrypt/decrypt lifecycle, v4/v5 gating, import/export validation.

---

## Stream 2 — ATT/GATT

All 10 files `-Wall -Wextra` clean; commit introduced no mechanical breakage. New read-path plumbing verified correct (see bottom).

### HIGH — ✅ FIXED
- [x] **C2-H1 (PRE-EXISTING) — Execute Write rejects Write Long that grows an attribute.** ✅ FIXED (validate against per-handle running length grown by earlier fragments; value_maxlen retained) `att_server_dispatch.c:1370` validates `pe->offset > a->value_len` against the *static pre-commit* length, but apply loop (1553-1557) grows value_len per fragment. Empty writable char written via Prepare(0,18)+Prepare(18,18)+Execute fails 2nd fragment with Invalid Offset — Write Long (Vol 3 Part G §4.9.4) can't grow past first fragment. Fix: validate against a per-handle running length or `value_maxlen` only (1377 already checks maxlen).
- [x] **C2-H2 (PRE-EXISTING) — ATT client max-skip abort desyncs the bearer.** ✅ FIXED (both abort branches call `att_bearer_fail()` per §3.3.3) `att.c:635-653`. After 16 unsolicited PDUs awaiting a response on the fixed bearer, releases the pending slot + returns EBADMSG WITHOUT failing the bearer while the real response is in flight; next `att_request` recv()s the stale response → if opcodes match, wrong data returned + persistent desync. §3.3.3 requires failing the bearer. Fix: fail/reset the bearer on abort.

### MEDIUM/LOW — ✅ ALL FIXED except C2-M10 (deferred: HOGP report-mode lives in `blued_central.c`, blued-core agent's file)
Fixed: C2-M3 (Write Command Error Response suppressed for command opcodes), C2-M4 (multi-HVN falls back to individual truncating notifications), C2-M5 (REGRESSION: DYNAMIC read-by-type breaks list cleanly, no terminal 0x02; AUTHORIZE still denies), C2-M6 (REGRESSION: new `att_check_security_perms_read()` masks read-side bits for CCCD/notify gating), C2-M7 (REGRESSION: AUTHORIZE rejected at Prepare + execute-commit deny), C2-M8 (ctl GATT Read Long continuation), C2-M9 (txn not freed on per-verb error), C2-M11 (unsubscribe marks pending before dropping lock), C2-L1 (Find By Type Value uses per-conn CCCD), C2-L2 (Write MTU check), C2-L3 (SET_VALUE targets scratch DB in txn), C2-L4 (hash recompute for full value-inclusion set), C2-L5 (real op-timeout wired), C2-L6 (MTU_REQ forced to fixed channel), C2-L7 (comment fix). **C3-H1 (ctl_gatt.c part): redundant post-pairing wait removed after verifying smp_pair confirms encryption internally.**
<details><summary>original items</summary>
- [x] **C2-M3** ✅ **C2-M4** ✅ **C2-M5** ✅ **C2-M6** ✅ **C2-M7** ✅ **C2-M8** ✅ **C2-M9** ✅ **C2-M11** ✅ **C2-L1–L7** ✅
- [~] **C2-M10** — HOGP Report-mode one-sided: DEFERRED to blued-core agent (`blued_central.c:957-966`).
_(superseded detail below)_</details>
- [ ] ~~**C2-M3 (PRE-EXISTING) — Write Command can elicit an Error Response.**~~ `att_server_dispatch.c:611-613` `att_begin_defer` busy branch sends Error Response with the request opcode even for Write Command (0x52) → §3.4.5.3 violation. Out-of-line path guards via `pending_req_has_response`; busy branch doesn't.
- [ ] **C2-M4 (PRE-EXISTING) — Multiple HVN drops entries and reports success.** `att_server_notify.c:205-220`. No tuple fits MTU → loop breaks i==0, returns 0, caller counts delivered though nothing sent. Fix: fall back to individual truncating notifications.
- [ ] **C2-M5 (REGRESSION, our A-F1) — inline-read returns Read Not Permitted (0x02) for a readable DYNAMIC app char on aggregate paths.** `att_server_dispatch.c:92` `att_check_inline_read`. 0x02 means "not readable at all"; clients treat it terminal and never retry, so Read-by-UUID of a vendor characteristic fails permanently. The "make client retry with Read Request" intent needs a different mechanism/code.
- [ ] **C2-M6 (REGRESSION, our A-F2) — CCCD gated on merged read|write encryption.** `att_server.c att_check_security_perms` `need_enc = perms & (READ_ENCRYPT|WRITE_ENCRYPT)`. A plaintext-readable/encrypted-writable Notify char rejects an unencrypted client's CCCD write (0x0F) even though the notified payload is freely readable. Gate CCCD/notify on the NOTIFY-relevant (read) security, not merged.
- [ ] **C2-M7 (REGRESSION, our A-F1 — gap vs commit claim) — queued-write path bypasses per-access AUTHORIZE.** `att_server_dispatch.c:1227-1596`. Prepare+Execute never tests `ATT_ATTR_F_AUTHORIZE` (direct Write defers for app allow/deny at 1052); new owner-notify (1580) reports an unauthorized commit as done. Commit claimed authorization "on all queued-write paths" — this is the residual.
- [ ] **C2-M8 (PRE-EXISTING) — ctl GATT read truncates long values, no Read Long fallback.** `ctl_gatt.c:192` single `att_read()`; 512-byte value at MTU 23 reported complete at 22 bytes. `att_read_blob` exists (blued_central uses it); ctl path never does.
- [ ] **C2-M9 (PRE-EXISTING) — GATT registration txn freed on any per-verb error → later verbs hit LIVE DB.** `ctl_gatt.c:1534` (+1436,1472,1601,1646). Benign NOT_FOUND tears down the txn; next verb applies to live DB one-by-one with mid-apply hash recompute + Service Changed — the partial application BEGIN/COMMIT exists to prevent. Fix: keep txn intact or poison it, don't fall through to live.
- [ ] **C2-M10 (PRE-EXISTING) — HOGP Report-mode entry one-sided.** `blued_central.c:957-966` breaks after first Protocol Mode instance + single `protocol_mode_handle` cached (last wins); dual-HID composite gets one service into Report mode. (hogp_boot.c Boot path fix verified correct — writes all instances.)
- [ ] **C2-M11 (PRE-EXISTING) — Unsubscribe/subscribe CCCD race.** `ctl_gatt.c:472-487`. Last-owner unsubscriber drops `ctl_clients_lock` with sub still present/non-pending, then writes CCCD=0; concurrent subscriber sees it, concludes shared, adds sub without rewriting CCCD → sub that never gets notifications after CCCD=0 lands. Needs symmetric pending-removal marking.

### LOW
- [ ] **C2-L1** Find By Type Value bypasses per-conn CCCD + exact-length match only (`att_server_dispatch.c:1644-1658`). **C2-L2** Write Request length not MTU-checked (997-1027, theoretical). **C2-L3** GATT_SET_VALUE during txn writes live DB, reverted at COMMIT (`ctl_gatt.c:1394-1421`). **C2-L4** 0x2900 hash-recompute misses declaration attrs (Primary Service decl, owner_fd==-1). **C2-L5** ctl 2s ATT op-timeout dead code — runs at 30s ceiling (`ctl_gatt.c:190`/`att.c:606`). **C2-L6** `att_exchange_mtu` may route MTU_REQ over EATT (benign today). **C2-L7** comment/code mismatch `att.c:1421` (says 23, is 21; code right).

### Verified CORRECT (stream 2)
New read-path plumbing (`att_read_value`/`att_check_inline_read` set *outlen all branches, guard vptr!=NULL, AUTHORIZE/DYNAMIC deferral before helper on normal Read/Blob; CCCD per-conn sourcing on all 5 read paths; owner-notify once-per-handle commit-only CCCD-excluded). 0x00 remap peer-only→0x0E. Prepare verbatim echo + over-MTU reject (max-size part accepted). MTU cap 517 both sides, floored 23, EATT rejects MTU_REQ. DB hash (att_server_hash.c) correct incl. value-inclusion set + Service Changed. adv_builder guards don't reject valid inputs (exact-31-byte fill accepted). gatt.c discovery continuation no off-by-one/wrap. hogp_report bounds-clamped. Execute-write "partial tail" notify is correct-by-design (prepared writes are partial, not truncating).

## Stream 3 — HCI/GAP/conn

Every HIGH claim adversarially re-verified against source. `[V]`=personally traced.

### HIGH — ✅ ALL FIXED
- [x] **C3-H1 (CRITICAL) — central pairing reported as FAILURE.** ✅ FIXED (removed redundant outer `hci_wait_encryption` in blued_central.c + blued_peripheral.c success branch + ctl_gatt.c; agent re-verified EVERY smp_pair/smp_respond sub-path confirms encryption internally before removal) `smp_pair()` already waits for + consumes the HCI Encryption Change event internally (`smp_sc.c:1095`, `smp_sc.c:594`, legacy `smp.c:1436`); callers then call `hci_wait_encryption()` a SECOND time on the same one-shot event → inevitable timeout. `blued_central.c:220` maps completed pairing → `return -1`; `ctl_gatt.c:126` PAIR/elevate always fails; peripheral `blued_peripheral.c:577` never opens the ATT gate. `hci_wait_encryption` has NO already-encrypted short-circuit. Our H-H4 fix makes that dead 10s wait hold `hci_devreq_mutex` → freezes all HCI for 10s/pairing. **Verify regression-vs-preexisting**, but fix is clear: delete the redundant outer `hci_wait_encryption()` calls (encryption already confirmed inside SMP).
- [x] **C3-H2 (REGRESSION — our H-H1 incomplete) — TOCTOU pairing-worker hogp UAF.** ✅ FIXED (att_ops_begin moved to main thread before pthread_create + re-validate hogp; setup_worker_count guard prevents double-spawn) `blued_central.c:278-282` checks `hogp!=NULL` then `att_ops_begin`; disconnect (`blued_event.c:1761`) + APTO sweep read `att_ops_active` with a bare load, and att_ops is only bumped INSIDE begin(). Worker descheduled between check and begin() → teardown sees 0 → `free(dev)` → worker resumes into `smp_open`/`smp_pair` on freed memory. The exact UAF H-H1 claimed to fix. Fix: `att_ops_begin` at spawn on the main thread + re-validate hogp, or lock the teardown's att_ops check.
- [x] **C3-H3 (REGRESSION — our H-M1 incomplete) — reconnect branch leaves `con_handle_valid=true`.** ✅ FIXED (clear con_handle_valid=false in reconnect branch) `blued_central.c:44-65` NULLs `conn->att` but never clears `con_handle_valid`. Post-handle setup failure with reconnect → stale handle live for 3s→max backoff → LTK Request replies wrong peer's LTK, APTO frees the RECONNECTING conn, Enhanced Conn Complete hijacked. `blued_conn_disconnect` DOES clear it; this sibling path doesn't. Fix: clear `con_handle_valid=false` in reconnect branch.

### MEDIUM/LOW — ✅ FIXED (C3-M1 store-then-load; C3-M2 dedicated timer ident; C3-M3 bt_devreq copies min(rlen,n)+reports actual len; C3-M4 ext-scan success tracked; C3-M5 mesh resume all paths; C3-M6 privacy repopulates from bond_db+runtime_resolv; C3-M7 reslist_lock held; C3-M8 pid copied under lock; C3-M9 ISO fd+generation+client-gone; C3-M10 assessed/documented; C3-L batch: unlock-by-slot, accept 0x02/0x03, real errno, adv-registry clear, NO_DEDUP legacy, MONOTONIC, vhci probe). Skipped w/ reason: `blued_conn_addr_context` addr_type (its job is to resolve addr_type; caller has none). **C1-H2** ✅ FIXED (smp_sc_oob_clear moved post-pairing). **C2-M10** ✅ FIXED (Report mode written to all Protocol Mode instances; single cached handle limitation noted).
<details><summary>original items</summary>
- [ ] ~~**C3-M1..M10, C3-L**~~ `blued_event.c:1761` load-then-store (inverted vs the sweep's documented store-then-load at :1128). Worker retires between load+store → nobody signals pipe → deferred disconnect stranded (indication timeout → conn up forever). Fix: store disconnect_pending first, re-check att_ops_active.
- [ ] **C3-M2 (REGRESSION — our C-m1) — accept-retry timer ident collides with connection timers. `[V]`** `ctl.c:5628` arms EMFILE retry timer with ident `ctl_fd`; all other timers use `blued_next_timer_id` from 1 (fd range). kqueue keys by (ident,filter) → EV_ADD collision modifies existing knote → a conn timer clobbered (disconnect never fires) or retry overwritten (listener never re-enabled). Fix: draw ident from `blued_next_timer_id` or start counter above fd range.
- [ ] **C3-M3 (REGRESSION — our H-H3) — `bt_devreq` returns false success on OVERSIZE Command Complete. `[V]`** `lib/libbluetooth/hci.c:291-299`: controller returns MORE bytes than `rlen` → `if(rlen>=n)` fails → no copy, rlen unchanged, `goto out` error=0. Combined with new `memset(rparam,0,rlen)` (`:248`) → caller reads status==0 over zeroed buffer (e.g. `hci_get_bdaddr` → all-zero BD_ADDR as success; its `rlen<sizeof` guard can't catch it, rlen unchanged). Zero-fill converted a usually-caught garbage into silent false success. Fix: in `rlen<n` case truncate-and-report real length or fail EIO.
- [ ] **C3-M4..M10 (mostly PRE-EXISTING) —** M4 empty ext scan falls through to legacy scan + misreports as "no adapter" (`ctl_conn.c:117-129`); M5 mesh scanner left permanently off after failed SCAN (`ctl_conn.c:146-156`); M6 PRIVACY toggle drops persisted non-bond reslist entries + resolution flip-flops (`blued.c:1396-1420`) [our H-H5 rule not applied to toggle path]; M7 `blued_privacy_set` mutates reslist shadow without `reslist_lock` racing SMP workers; M8 `pid` pointer into bond DB escapes `bond_db_lock`, deref after unlock (`ctl.c:3560-3609`) [our H-M5 area]; M9 ISO CIS fd handout resolves client by raw fd, no generation, not cleared on gone (`iso.c:1019`); M10 `hci_wait_encryption` discards other-handle Encryption Change events (now normal-case via H-H4 serialization) + `IPC_ISO_EV_FAILED` rejected by bundled libble client.

### LOW (compact)
- [ ] **C3-L (batch):** event-loop trylock busy-spins 100% CPU while worker holds per-fd mutex (amplified by H1's 10s); `hci_send_raw_cmd`/hci_scan unlock-by-relookup wrong slot on fd reuse; `hci_le_ext_create_connection` rejects Peer_Address_Type 0x02/0x03 (connect-by-identity to RPA peer); `hci_wait_encryption` ETIMEDOUT masks all errors; `ctl_gatt_job_cancel` omits defer-teardown re-signal; `blued_conn_addr_context` matches without addr_type; Adv Set Terminated 0x12 leaves ctl adv-set `enabled` true + set_*_adv_enable skip H-H3 zero-fill; NO_DEDUP not honored in legacy branch of ext-scan loop; SCAN rate-limiter uses wall-clock `time()` not MONOTONIC; l2cap conn-param probes ubt0..7 not vhci* (test rig); ISO polish (double reply, MTU 0, dropped peripheral CIS, EMFILE partial-failure unchecked).

### Verified CORRECT (stream 3)
**H-H4 mutex discipline** (acquired once, released on every exit; event loop only trylocks — no lock cycle) `[V]`. **H-M6 ext-adv fragmentation** byte-exact (≤251 Op 0x03; >251 Op 0x01/0x00/0x02 correct offsets; clen=4+len; 252/502/1650 boundaries) `[reviewer, traced]`. **Adv Set Terminated 0x12** length/match/state-clear (only the ctl-registry desync is a gap). **Rate-limiter + duration cap (C-M1)** 4-per-10s never permanently disables, clamp 1..5s, both call sites seconds, `duration_sec*100` overflow-guarded. **`hci_get_con_handle` addr_type** all 3 sites pass correct BDADDR_LE_*. **H-M1 as applied in `blued_conn_disconnect`**, conn-complete/LE-meta matching, epoch gating, att-ops begin/end balance (no underflow) `[V]`. HCI packing/byte-order/opcodes, hci_log math, ISO CIG/CIS/BIG packing, ctl framing/partial-IO.

## Stream 4 — mesh crypto/net/transport

### MEDIUM — ✅ ALL FIXED (harness passes on fix, fails on revert)
- [x] **C4-M1 (REGRESSION, our M-L6 wrapper) — `mesh_rpl_net_receive_ivupd` gates IV-1 on the iv_update flag; spec accepts IV-1 unconditionally.** ✅ FIXED (retry IV-1 whenever iv_index>0 regardless of iv_update; replay verdict still authoritative; header contract updated) `mesh_rpl.c:150-156`, contract `mesh_rpl.h:100-114`. Retries `iv_index-1` only when `iv_update!=0`, but MshPRT 3.10.5/3.11.5 IVI-selection applies in Normal Op too — and the library agrees with itself (`mesh_iv_rx_accept` mesh_iv.c:49-60, `try_decrypt` mesh_sim.c:1148-1153 both try IV-1 unconditionally). Node A at IV=n (Normal, passes iv_update=0); peer B still In-Progress (allowed ≥96h) sends IV=n-1 → wrapper tries only n → IVI check fails → valid traffic dropped up to 96h. Fix: retry IV-1 whenever `iv_index>0` (or fix the header contract). Mechanically otherwise correct (IVI ensures one candidate; RPL enforced with authenticating IV; replay verdict correctly doesn't retry).
- [x] **C4-M2 (PRE-EXISTING) — `mesh_sim_send_beacon` beacons with NEW NetKey in KR Phase 1, collapsing subnet to Phase 3.** ✅ FIXED (`phase >= MESH_KR_PHASE_2` both branches; Phase1→Phase3 collapse eliminated) `mesh_sim.c:2101-2103,2110-2112` (`phase >= MESH_KR_PHASE_1 ? new_netkey : netkey`). Per MshPRT 3.11.4 Phase-1 nodes beacon with the OLD key; new-key beacons begin Phase 2 (KR=1). Here a Phase-1 node emits a new-key beacon with KR=0 = the Phase-3 signal → receiver parses under new_netkey, `mesh_kr_beacon(flag=0)` moves Phase1→Phase3, `key_refresh_finalize` revokes old key mid-distribution, cutting off not-yet-updated nodes. Fix: `phase >= MESH_KR_PHASE_2`.

### LOW — ✅ ALL FIXED
- [x] **C4-L1 — Device sends Public Key PDU even when Start selected OOB.** ✅ FIXED (gated on `start_val[1]==0`)
- [x] **C4-L2 — RP server can't report device-initiated close while ACTIVE.** ✅ FIXED (ACTIVE accepted → LINK_CLOSED_BY_DEVICE report)
- [x] **C4-L3 — RP parsers return -1 with partial output.** ✅ FIXED (memset before early -1)
- [x] **C4-L4 — completed segmented txn re-ack.** ✅ FIXED (added `complete` flag; retransmit re-sends block ack per 3.5.3.4)

### Verified CORRECT (stream 4)
All df027e4bcea crypto changes (CCM INT_MAX guards + bad-miclen zeroization honor zero-output contract; k2 plen==0 reject; aes_cmac NULL guard zeroes mac; net_rpl_check deprecation comment-only). All 4 nonce builders byte-exact (3.8.5); k2/k3/k4 derivation; CCM AAD/tag ordering; PECB layout + self-inverse XOR; CTL-dependent transport bounds + MIC sizes; IVI gate; build/parse round-trips. Transport SZMIC/SeqZero/SegO/SegN packing, blockack off-by-one-free, SAR ceil-div, idempotent reassembly, seg-ack layout. RPL strict (IV,SEQ) ordering, fail-closed. IV state machine (TX n-1 during update, 96h dwell, ≤42 recovery). Beacons (secure CMAC-8; private-beacon B0/C0/C1-as-CCM). Provisioning ConfirmationInputs 145-byte layout both roles + both algorithms, SessionNonce, M-F1 reflection rejects only byte-exact equal keys, FCS CRC-8, GP segmentation, dup-txn re-ack. nmc cache (M-N1) key/compare consistent. SeqAuth reconstruction + originate SEQ arithmetic.

## Stream 5 — mesh models + DF

**Strongest verification: agent compiled the real sources `-Wall -Wextra` clean AND executed round-trip/behavior harnesses.** Every df027e4bcea change (P-C1a-d DF wire formats, P-H4/H5 bindings, P-M9-14 models) verified correct BY EXECUTION against MshPRT/MMDL worked examples. **No high/medium defects.**

### LOW (all PRE-EXISTING, limited reachability) — ✅ ALL FIXED (harness-verified)
- [x] **C5-L1 — CTL Status writes stale Target when only sibling transition active.** ✅ FIXED (`.active ? target : present` per field; harness proves Target Temp=0x3000 with only delta-UV active) `mesh_lighting.c:797-805` (+914-918): `ctl_transition_active(srv,1)` gates on temp OR delta-UV but target written unguarded; present 0x3000 + only delta-UV active → Target=0x0000 not 0x3000. Parallel paths (811-819, 896-904) guard `.active ? target : present`. Low: handler starts both transitions in lockstep, divergence needs external API/tick interleaving.
- [x] **C5-L2 — `MESH_DF_RECV_FOR_ORIGIN` unreachable via documented origin flow.** ✅ FIXED (e==NULL branch returns FOR_ORIGIN when node_covers path_origin; pre-seed test + mesh_sim path unaffected) `mesh_df.c recv_path_reply`: PATH_REPLY only matched against existing table entry; `mesh_df_discovery_start` never seeds the table → origin following the API gets `RECV_DROP`. Shipped consumers work around it (test pre-seeds, mesh_sim.c:1482 calls `discovery_on_reply` directly), so unaffected — but the header contract is easy to violate.
- [x] **C5-L3 — target-reply range truncation** ✅ FIXED (clamp addr_last to addr+0xfe in mesh_df_node_init) — `mesh_df.c:1010` `range_length=(uint8_t)(addr_last-addr+1)` truncates for element span >255 (`mesh_df_node_init` doesn't validate). Unreachable with realistic element counts.

### Verified CORRECT by EXECUTION (stream 5)
P-C1a range LP=bit15/RangeStart-low15-BE (0x1234+5→`0x92 0x34 0x06`, both 2/3-octet forms, overflow+RangeLength<2 rejected). P-C1b PATH_REPLY flags 7/6/5, Path_Origin-before-FN, conditional ranges (Table 3.52 byte-exact); PATH_REQUEST octet-0 (Table 3.51). P-C1c/d relay re-originate TTL0→0xFFFB, metric+1 saturating, dup consumed, better-metric lane re-processed, Echo TTL0x7F on forward bearer, DEP_UPDATE Type bit7, fixed-group dests rejected/group+virtual accepted, 0xFF DoNotProcess survives round-trip. P-H6 `(RangeStart<<1)|LP` LE mirror (`0x69 0x24 0x06`). P-H4 bindings (OnOff→Lightness Default-first, Level→Lightness/CTL-Temp/HSL exact at min/mid/max, in_bind stops recursion both directions). P-H5 0x3F→DTT. P-M9 scene current=0 during transition. P-M10 pub-ceil vs sub-floor genuinely different + both correct (3→3 vs 3→2, 0x579→0x0C). P-M11 disabled hops=0. P-M12 Last latched only settled-nonzero. P-M13 prohibited ignored (HSL wrap-hue correctly ACCEPTED). P-M14 add→both, clear_current/clear_faults split. Plus: heartbeat, transition-time codec, opcode 1/2/3-octet, key-index packing, Sensor MPID A/B, scheduler 80-bit, Time authority bit, DF forwarding-number serial arithmetic.

## Stream 6 — meshd + friend/mgr/proxy  (PARTIAL — friend/LPN detail pending)

**Verdict: P-H7 (friendship) and P-M15 (AppKey rotation) were BROKEN as shipped — now ALL FIXED surgically (no revert needed), harness-proven (friendship establishment/delivery/SEQ, AppKey rotation across save/reload+promote, Directed Proxy parse under ASan/UBSan).** P-H8 + C-C1 were already correct.

### HIGH — ✅ ALL FIXED (harness-verified)
- [x] **C6-H1 (REGRESSION, P-H7) — every Friend Update sent with SEQ=0.** ✅ FIXED (`meshd_friend_send_msg` stamps `np.seq=self->seq` + advances for is_update entries; harness: 5 successive Updates accepted, pre-fix SEQ=0 reproduced+rejected) `mesh_friend.c:879-910` `friend_build_update_entry` never assigns `e->seq`; `meshd_node.c:4740` `np.seq=e->seq`, `meshd_friend_send_msg` never advances `self->seq`. LPN establishes on first Update (SEQ0), every later empty-queue Update is SEQ0 → LPN network RPL rejects as replay (`mesh_rpl.c:79-84`) → Poll never answered → PollTimeout → friendship-lost → re-Request → infinite churn. Fix: assign a monotonic Friend SEQ to the Update.
- [x] **C6-H2 (REGRESSION, P-M15) — AppKey Update distributes an all-zero new key after any meshd restart.** ✅ FIXED (persist v6→7 serializes appkey_new in embedded frame; pre-v7 migrates by minting fresh; harness: survives save/reload, distributes real key) `meshd_persist.c:585-609` `encode_body` omits `appkey_new`; `:1119` `decode_body` calloc → zero; `meshd.c:146-149` re-saves mirror from zeroed embedded copy, destroying the correct v3-mirror `appkey_new`. create→restart→`cfg appkey-update` → `mesh_manager.c:1065` distributes 16 zero bytes. Fix: serialize/restore `appkey_new` in the embedded frame (it only round-trips through the mirror today).
- [x] **C6-H3 (INCOMPLETE FIX, P-M15) — key refresh can never complete: `appkey_new` never promoted to current or re-minted.** ✅ FIXED (new `mesh_mgr_appkey_promote` + `meshd_appkey_finalize` + `key-refresh appkey-finalize` ctl verb; swaps Provisioner primary so restart consistency check holds; harness: 2nd Update distributes new key, no Cannot Update). NOTE: node-side AppKey model is single-key (immediate-replace, not true two-phase KR) — pre-existing, out of scope. No code (`mesh_manager.c`, `meshd_ctl.c:1192-1264`, `meshd_cfgclient.c`) does `appkey := appkey_new` + re-mint after phase 3. → `appkey_add_pdu` hands revoked old key to new nodes; 2nd refresh redistributes already-promoted key (nodes reply Cannot Update); post-rotation restart consistency check `timingsafe_bcmp(mgr->appkey, appkey,16)` (`meshd_persist.c:1180`) fails → meshd refuses to boot ("node state corrupt"). Fix: implement the phase-3 promotion + re-mint.
- [x] **C6-H4 (REGRESSION, P-H7) — Friend Clear Confirm sent to wrong destination at TTL 0.** ✅ FIXED (stamp `out->addr=src`; SEND_CONTROL with set addr routes via managed flooding TTL 0x7F) `mesh_friend.c:1290-1300` builds Confirm as `ACT_SEND_CONTROL` but never sets `out->addr`; `meshd_node.c:4914-4915` routes all SEND_CONTROL to `lpn_addr` at TTL 0. §3.6.6.3.1: Confirm must go to the Clear's sender (`np.src`, possibly multi-hop). Previous Friend never gets Confirm.
- [x] **C6-H5 (REGRESSION, P-M16) — Directed Proxy Control parse: OOB read of `in[1]` before length check.** ✅ FIXED (`if(inlen<2) goto bad` before in[1]; proven with malloc(1) under ASan) `mesh_proxy.c:553` reads `in[1]` while function only guarantees `inlen>=1` (497); inlen checks come after (557/561/567). 1-octet PDU (opcode only) → 1-byte OOB read steering parsing. Fix: length-check before field read (every sibling case does).

### MEDIUM/LOW — ✅ ALL FIXED or DOCUMENTED (harness-verified)
Fixed: C6-M6 (`mesh_lpn_fsm_on_message` now called from `meshd_bearer_rx` → FSN toggles, queue advances; harness: 3 msgs in order no loss), C6-M7 (Clear Confirm 0x06 admitted, rpl_slot 7→8 via nitems), C6-M8 (Update carries network-current IV + real kr flag), C6-M9 (queue RX tries new_enckey during KR), C6-M10 (SAR `rx_started_ms` refreshed per segment), C6-L12 (proxy range unicast-validated like DF), C6-L13 (v2 mirror accepted+migrated, no boot fail). Documented: C6-M11 (ReceiveDelay honored LPN-side via establish_window; real-radio Friend defer documented), C6-L14 (segmented friend-queue pre-existing gap).
<details><summary>original items</summary>
- [ ] ~~**C6-M6 (PRE-EXISTING) — `mesh_lpn_fsm_on_message` has zero callers in meshd.**~~ Queue-delivered access PDUs (ctl=0) never toggle LPN FSN → every Poll same FSN → Friend resends queue head forever, later entries never delivered.
- [ ] **C6-M7 (REGRESSION, P-H7 wiring) — Friend Clear Confirm (0x06) missing from meshd RX admit gate** `meshd_node.c:4867-4896`; received Confirm dropped → initiator clear-repeat only stops at 2×PollTimeout deadline, not on Confirm.
- [ ] **C6-M8 (PRE-EXISTING) — Friend Update carries wrong IV Index / never signals Key Refresh.** `meshd_node.c:4911-4912` hard-codes `key_refresh=0` + passes Poll's decrypt IV not network-current. LPNs adopt IV/KR only from Updates (§3.6.5.2) → never adopt new IV/NetKey.
- [ ] **C6-M9 (PRE-EXISTING) — Friend access-queue RX decrypts only with old key during KR** `meshd_node.c:4948-4951` (control path 4841-4853 also tries new_enckey). New-NetKey messages lost for LPN.
- [ ] **C6-M10 (INCOMPLETE FIX, P-M16) — 20s SAR reassembly timeout not per-segment.** New lib `mesh_proxy_reasm_tick` (`mesh_proxy.c:209-228`) is correct but HAS NO CALLER; daemon still uses `meshd_proxy_gatt.c:164-167` stamping `rx_started_ms` only on first segment, torn down 20s after first (`:397-401`). Multi-segment transfer with sub-20s gaps but >20s total dropped (§6.3.2.2). Fix: call the library tick, or refresh the daemon timer per segment.
- [ ] **C6-M11 (PRE-EXISTING) — ReceiveDelay negotiated but never honored** `mesh_friend.c:978` stored, read nowhere; poll responses sent synchronously → conformant LPN misses every response.
- [ ] **C6-M15 (REGRESSION, our C-m3) — `drain_tx` drops one-shot PDUs on transient ENOBUFS over a healthy link.** `meshd_node.c:573-576`. `meshd_blued_tx` keeps link up, returns -1 on ENOBUFS; rewritten drain counts tx_error then discards (old code compacted + retried next tick). Casualties: Status replies, unacked originates, group segmented msgs. Drop-on-bearer-DOWN is right; ENOBUFS-healthy-link is collateral. Fix: distinguish ENOBUFS (retry) from bearer-down (drop).

### LOW
- [ ] **C6-L12** Directed Proxy Control range accepted without unicast validation (`mesh_proxy.c:561-576`; DF validates the identical structure). **C6-L13 (REGRESSION, P-H8/P-M15 v3 bump)** no v2→v3 migration for manager mirror (`mesh_manager.c:658`) → boot hard-fail until file hand-deleted, losing roster+DevKeys. **C6-L14 (PRE-EXISTING)** segmented friend-queue delivery impossible (no SAR-requeue path; `send_msg` caps 16 bytes).

### Verified CORRECT (stream 6)
**C-C1 `node_rehome_sim`** — every self-referential pointer in struct mesh_node/meshd_node/mesh_sim enumerated vs mesh_sim.h: `n->sim`, `rpl.entries`, `elems[].models/subs/labels`, `friend_rpl[].entries`, `self`, devkey args ALL rebased to nd; `meshd_sync_subscriptions(nd)` re-points config views after copy; loop bounds + self_index correct; nothing left aiming at dead tmp. **P-H8 SEQ reserve** — save persists seq+RESERVE, load resumes with no second add, strictly monotonic across crash, overflow fails safe, no double-reserve. **appkey_new minting** (RAND_bytes at create, checked). **Private identity adv build** (hash inputs + ordering + 21-octet AD). **Friend Clear timers** (1s doubling repeat, 2×PollTimeout stop, stop-on-Confirm, stop-on-termination, Clear addressed to prev Friend). **Queue flush on termination** (no UAF/leak). **Segmented-enqueue gate/SEG bit**. **LPN establishment logic in isolation** (correct but non-functional due to C6-H1). **meshd_fault_snapshot health split**, **SEND_CLEAR addressing/TTL/0x05**, **aggregator recursion guard**, **proxy SAR framing bounds**, **directed-proxy cap-status parse**.
