# Start Oracle directly as PID 1.  The remaining entries are executable
# recovery candidates if the kernel cannot execute Oracle at all.
init_path="/sbin/oracle-init:/sbin/init:/sbin/init.bak:/rescue/init"
