# Start Authority directly as PID 1.  The remaining entries are executable
# recovery candidates if the kernel cannot execute Authority at all.
init_path="/sbin/authority-init:/sbin/init:/sbin/init.bak:/rescue/init"
