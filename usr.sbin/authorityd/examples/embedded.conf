# /etc/authorityd.conf - Embedded / Appliance profile
#
# Locked-down configuration for appliances and embedded devices.
# All system operations are gated - no process can reboot, load
# kernel modules, or modify boot environment variables without
# an authorized capability token.  All privileged ports are claimed.
#
# Services that need these capabilities must declare them in
# their .cap bundles and receive tokens from the Authority.

integrity {
    ptrace = true;
    signal = true;
    visible = true;
    wait = true;
    sched = true;
    core = true;
    ktrace = true;
}

claims {
    # All privileged ports
    network = [
        {
            port = ".<1024";
            protocol = "*";
            direction = "bind";
            domain = "*";
        },
    ];

    # All sensitive system operations require tokens.
    system = [
        "reboot",
        "kldload",
        "kldunload",
        "kenv",
    ];
}
