# Start Capsule (the PID 1 personality of authorityd) directly as PID 1.  The
# remaining entries are executable recovery candidates if the kernel cannot
# execute Capsule at all.
init_path="/sbin/capsule:/sbin/init:/sbin/init.bak:/rescue/init"
