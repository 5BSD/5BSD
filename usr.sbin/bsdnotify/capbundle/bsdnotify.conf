# Notify authority is granted by the administrator and keyed by the
# immutable serviced manifest label.  Undeclared labels are default-deny.
#
# The former org.5bsd.system.reboot/rebootd publisher of the
# system.shutdown.* events was removed with the rebootd daemon (its
# lifecycle role migrated to authority-init's authenticated control socket).
# Re-add a publisher grant here if those notifications are re-homed.
clients {
};
